Die Programmiersprache ist streng Klassen/Objekt orientiert.
Das bedeutet, dass eine Variable vom Datentyp Interger ein Objekt der Klasse
Fixum ist, usw.
Die jeweilige Klasse bietet für das Objekt vorgefertigte Methoen an.

var="teststring"
var.class
	#die Methode class auf das Objekt var angewandt gibt die Klasse des Objektes aus

var.reverse
	#die Anwendung der Mehtode reverse erzeugt eine Ausgabe in der der String umgekehrt wird. Der Wer
	der Variablen ändert sich nicht.

var.reverse!
	#ein ! am Ende einer Methode ändert den Variablenwert

var.center(30)
	#Methoden erwarten manchmal einen Parameter

	